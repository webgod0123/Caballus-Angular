import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.caballus.app',
  appName: 'ion-caballus',
  webDir: 'dist/apps/ion-caballus',
  bundledWebRuntime: false
};

export default config;
